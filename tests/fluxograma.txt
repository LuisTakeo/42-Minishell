- tokenizar
-- separar as palavras/tokens
-- incluir validação das aspas
-- incluir separação por símbolos especiais (criar fubção/regra pra cada tipo)
-- definir tipos de token
-- expandir variaveis
-- verificar se tokens são validos (validação do comando)
- verificar se é um builtin
-- se sim, executar função de acordo
-- se não, utilizar fork para execve


-- função

- fork -> processo pai / processo filho
- verificar se é processo filho
- - void exec(t_list *param, int id)
- - se tiver mais de um comando (pipe)
- - 	fork ->
